import 'package:flutter/material.dart';

class FriendAdd extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("友達追加"),
      ),
      body: Center(child: Text("友達追加")),
    );
  }
}
